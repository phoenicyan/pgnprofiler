#pragma once

string concatIth(const vector<string>& names, size_t i, size_t span);
string RunExecutor(const string& fileNames, const string& directory, int repeat = 0);
void LogFile(const CHAR* cFileName, size_t& files, size_t& messages);
